# emptied out
# configs read from desktop-deployment.yaml -> https://github.com/makcalif/config-properties.git

spring:
  application:
    name: auth-service
  data:
    mongodb:
      host: 34.67.72.80
      port: 80
      database: oauth-db
      username: oauth-user
      password: password

server:
  port: 8012
  servlet:
    context-path: /uaa
logging:
  level:
    org.springframework: DEBUG
